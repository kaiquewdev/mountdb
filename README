MountDB

MountDB is a simple, python script to mount content of html or
text file and insert in a sqlite database.

"Future support for other databases will increase. But while the sqlite file can be converted."
